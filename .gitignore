# Dependencies
node_modules/
# Utan snedstreck också: mönstret med snedstreck matchar bara KATALOGER, och
# en symlänk som heter node_modules slank igenom och committades 2026-08-22.
# Den pekade på en absolut sökväg i en utvecklares hemkatalog, och på runnern
# lade `git reset --hard` tillbaka symlänken ovanpå det `npm ci` just
# installerat — så föll förslagskörningen på «Cannot find package 'tsx'».
node_modules
.pnpm-store/

# Build output
site/dist/
site/.astro/

# Pipeline reports
pipeline/.report/
.report/

# Environment
.env
.env.*
!.env.example

# OS
.DS_Store
*.swp

# Locks (inkludera pnpm-lock.yaml i CI)

# Pipeline etag-cache (lokal körning)
pipeline/.cache/

# Kopplingsköns källcache (lokal körning)
handlingsvagen/data/.kallcache/

# Låset över data/ — se pipeline/src/datalas.ts. Aldrig i repot: det beskriver
# en körning på den här maskinen, inte projektets tillstånd.
data/.datalas
